var fruit={
    name:"orange",
    amount:30,
    quantity:2,
};
console.log(fruit.quantity + " " + fruit.name + " for KES " + fruit.quantity*fruit.price);

//A function executes a set of instructions on data and return the result.
//Method-a set of instructions associated with an object.
//Objects are containers for named values.
var car={
    wheels:4,
    name:"toyota",
};
console.log(car.name + ""+car.wheels);





